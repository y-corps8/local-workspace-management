# `public/js/dom.js`

`els` map of `getElementById` / `querySelector` for the dashboard shell. Ids match [index.html](../index.md).

## Exports

`els` — one property per region (health, projects, console, confirm, Settings form).

## Tests

None.
