## Summary

<!-- What changed and why. -->

## Checklist

- [ ] `npm test` passes
- [ ] User-facing behavior: existing `docs/` page or root README updated if needed (no new user-facing page for a small `src/` helper)
- [ ] Added or renamed a `src/` or `public/` file: matching page under `docs/developers-guide/`
- [ ] No `workspace.json`, `.cache/`, or personal project paths in this PR
